--- a/src/clamp.cj
+++ b/src/clamp.cj
@@ -0,0 +1,11 @@
+package clamp_fixture
+
+public func clamp(value: Int64, minimum: Int64, maximum: Int64): Int64 {
+    if (value < minimum) {
+        return minimum
+    }
+    if (value > maximum) {
+        return maximum
+    }
+    return value
+}
