# Midpoint bug

`midpoint(left, right)` must return `(left + right) / 2` using Cangjie's `Int64`
integer division. Inputs used by this task are small enough that addition does not
overflow. Keep the existing public API unchanged.
