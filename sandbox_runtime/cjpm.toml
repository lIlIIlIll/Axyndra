[package]
  cjc-version = "1.1.0"
  name = "sandbox_runtime"
  organization = ""
  description = "Fail-closed workspace sandbox, environment filtering, and secret redaction"
  version = "1.0.0"
  output-type = "static"

[dependencies]
  "agent_domain" = { path = "../agent_domain", output-type = "static" }
  "sandbox4cj" = { path = "../libs/sandbox4cj", output-type = "static" }
