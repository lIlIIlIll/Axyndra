[package]
  cjc-version = "1.1.0"
  name = "app_mailbox_vnext_contract"
  organization = ""
  description = "bounded and coalesced typed App Protocol mailbox contract"
  version = "1.0.0"
  output-type = "executable"

[dependencies]
  "agent_app_protocol" = { path = "../../agent_app_protocol", output-type = "static" }
  "agent_domain" = { path = "../../agent_domain", output-type = "static" }

