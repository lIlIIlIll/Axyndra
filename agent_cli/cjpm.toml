[package]
  cjc-version = "1.1.0"
  name = "agent_cli"
  organization = ""
  description = "Typed command router for the product CLI"
  version = "1.0.0"
  output-type = "static"

[dependencies]
  "agent_domain" = { path = "../agent_domain", output-type = "static" }
