# subagent_runtime

Structured parent/child bookkeeping and budget enforcement. Planner, executor,
reviewer, and generic children are roles/configuration; every child receives an
`AgentRunner` built from the same `AgentCore`.
