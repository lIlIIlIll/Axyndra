[package]
  cjc-version = "1.1.0"
  name = "agent_app_protocol"
  organization = ""
  description = "Typed in-process application commands and events"
  version = "1.0.0"
  output-type = "static"

[dependencies]
  "agent_domain" = { path = "../agent_domain", output-type = "static" }

