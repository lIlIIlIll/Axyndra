[package]
  cjc-version = "1.1.0"
  name = "task_runtime"
  organization = ""
  description = "Task lifecycle independent from the agent loop"
  version = "1.0.0"
  output-type = "static"

[dependencies]
  "agent_domain" = { path = "../agent_domain", output-type = "static" }
